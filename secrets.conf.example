# ──────────────────────────────────────────────────────────────────────
# secrets.conf — Define your secrets and project mappings
#
# This file tells keychain-secrets-manager:
#   1. What secrets exist (name + description)
#   2. Which env var name each secret maps to
#   3. Which projects need which secrets in their .env files
#
# Format:
#   [secrets]          — one per line: keychain-name | ENV_VAR_NAME | Description
#   [projects]         — one per line: /path/to/project | ENV_VAR1, ENV_VAR2, ...
#   [env]              — global .env output path (written on every export)
#
# Lines starting with # are comments. Blank lines are ignored.
# ──────────────────────────────────────────────────────────────────────

[settings]
# Keychain service name (groups all your secrets under one label)
service = my-app

# Global .env file path (all secrets written here, sourced by your shell)
env_file = ~/.my-app/.env

# Log file path
log_file = ~/.my-app/logs/secrets-export.log

[secrets]
# keychain-name          | ENV_VAR_NAME          | Description
anthropic-api-key        | ANTHROPIC_API_KEY     | Anthropic Claude API key (sk-ant-...)
openai-api-key           | OPENAI_API_KEY        | OpenAI API key (sk-proj-...)
google-gemini-api-key    | GOOGLE_GEMINI_API_KEY | Google Gemini API key
notion-api-key           | NOTION_API_KEY        | Notion integration API key (ntn_...)
brave-api-key            | BRAVE_API_KEY         | Brave Search API key
database-url             | DATABASE_URL          | PostgreSQL connection string
stripe-secret-key        | STRIPE_SECRET_KEY     | Stripe secret key (sk_live_...)
stripe-webhook-secret    | STRIPE_WEBHOOK_SECRET | Stripe webhook signing secret (whsec_...)

[projects]
# /path/to/project       | ENV_VAR1, ENV_VAR2, ...
~/projects/my-web-app    | ANTHROPIC_API_KEY, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, DATABASE_URL
~/projects/my-api        | OPENAI_API_KEY, ANTHROPIC_API_KEY, DATABASE_URL, NOTION_API_KEY
~/projects/my-cli-tool   | BRAVE_API_KEY, ANTHROPIC_API_KEY
